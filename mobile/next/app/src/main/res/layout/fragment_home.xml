<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg"
    android:padding="24dp"
    android:fitsSystemWindows="true"
    tools:context=".ui.home.HomeFragment">

    <ImageButton
        android:id="@+id/btnRepo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_open_in_new_24"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/home_btn_repo"
        app:tint="@color/on_app_bg_60"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/centerBlock"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Meta Mind Solution"
            android:textColor="@color/on_app_bg"
            android:textSize="28sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"
            android:paddingBottom="8dp" />

        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/home_subtitle"
            android:textColor="@color/on_app_bg_60"
            android:textSize="16sp"
            android:paddingBottom="24dp" />

        <TextView
            android:id="@+id/tvFooter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/home_footer"
            android:textColor="@color/on_app_bg_60"
            android:textSize="12sp"
            android:gravity="center" />

        <ImageView
            android:id="@+id/image_logo"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:src="@drawable/logo_metamind"
            android:layout_marginTop="24dp"
            android:contentDescription="Logo da Equipe MetaMind"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>