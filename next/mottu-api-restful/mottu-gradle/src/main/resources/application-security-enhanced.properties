# ==================================================
# Security (prod/vps)
# ==================================================

# CORS por ambiente (configure via env)
mottu.security.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://seu-dominio.com}
mottu.security.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
mottu.security.cors.allowed-headers=*
mottu.security.cors.allow-credentials=true

# Headers recomendados (se aplicável via filtro)
server.forward-headers-strategy=framework
