# ==================================================
# CONFIGURAÇÃO SEGURA COM VARIÁVEIS DE AMBIENTE
# ==================================================

# --- Configuração Segura de Banco de Dados ---
# Use variáveis de ambiente para credenciais
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
spring.datasource.username=${DB_USERNAME:relacaoDireta}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- Configurações de Produção Seguras ---
# spring.datasource.url=${PROD_DB_URL:jdbc:oracle:thin:@localhost:1521:XE}
# spring.datasource.username=${PROD_DB_USERNAME:mottu}
# spring.datasource.password=${PROD_DB_PASSWORD}

# --- Configurações de Segurança Adicionais ---
spring.datasource.hikari.connection-test-query=SELECT 1 FROM DUAL
spring.datasource.hikari.validation-timeout=5000

# --- Configurações de CORS Seguras ---
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://72.61.219.15:3000,http://72.61.219.15:8080}
cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS,HEAD
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=7200
