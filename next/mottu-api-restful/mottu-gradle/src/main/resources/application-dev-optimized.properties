# ==================================================
# Development "turbo" tuning (opcional)
# ==================================================

# --- Hikari (dev minimal but snappy) ---
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.connection-test-query=SELECT 1 FROM DUAL
spring.datasource.hikari.auto-commit=false

# Tempo máximo p/ requisições assíncronas (ex.: streaming/download)
spring.mvc.async.request-timeout=120000
# Tomcat
server.connection-timeout=20000
server.tomcat.max-swallow-size=50MB
server.tomcat.accept-count=200
server.tomcat.max-threads=300
# Keep-Alive (quando suportado)
server.tomcat.max-keep-alive-requests=100


# --- Logs um pouco mais verbosos no dev normal (mantidos no arquivo logging) ---


